{
  "name": "brothers-fashion",
  "version": "1.0.0",
  "description": "Brother's Fashion — Premium Menswear & Custom T-Shirt Studio",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:zip": "vite build && powershell -Command \"Compress-Archive -Path dist/* -DestinationPath cpanel_deploy.zip -Force\"",
    "preview": "vite preview"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "vite": "^5.4.0"
  }
}